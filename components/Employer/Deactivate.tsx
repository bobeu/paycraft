import React from "react";
import Box from "@mui/material/Box";
import { EmployeePayload } from "@/contractApis/readContract";

export default function Deactivate({selectedData} : {selectedData: EmployeePayload}) {
    return(
        <section id="Deactivate">
            Deactivate
        </section>
    );
}