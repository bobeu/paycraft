import React from "react";
import Box from "@mui/material/Box";
import { EmployeePayload } from "@/contractApis/readContract";

export default function Activate({selectedData} : {selectedData: EmployeePayload}) {
    return(
        <section id="Activate">
            Activate
        </section>
    );
}
