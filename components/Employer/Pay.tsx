import React from "react";
import Box from "@mui/material/Box";
import { EmployeePayload } from "@/contractApis/readContract";

export default function Pay({selectedData} : {selectedData: EmployeePayload}) {
    return(
        <section id="Pay">
            Pay
        </section>
    );
}