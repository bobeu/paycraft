import React from "react";
import Box from "@mui/material/Box";
import { EmployeePayload } from "@/contractApis/readContract";

export default function Approve({selectedData} : {selectedData: EmployeePayload}) {
    return(
        <section id="Approve">
            Approve
        </section>
    );
}
